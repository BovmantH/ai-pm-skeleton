<desc_env>
# 当前系统与开发环境配置

> ⚠️ **首次使用请按本机情况修改本文件全部内容**——AI 会严格按这里写的发命令,信息错了它会一直给错。

1. **操作系统**：<!-- 例：Windows 11 Pro 26100 / macOS 14.5 / Ubuntu 22.04 -->
2. **终端约束**：<!-- 例：Windows PowerShell（禁止 Bash 特有命令）/ zsh / bash --> 提供的所有命令行指令必须完全兼容上面声明的 shell。
3. **核心环境版本**：
   - Shell：`<版本号>`
   - Git：`<版本号>`
   - Python：`<版本号>`（pip `<版本号>`）
   - Node.js：`<版本号>`（npm `<版本号>`）
   - <!-- 按需追加：Java / Go / Rust / Docker / kubectl 等 -->
4. **环境隔离**：在提供 Python 或 Node.js 的安装指令时，默认考虑虚拟环境（如 `venv`）或本地项目安装，除非明确要求全局安装。

<!-- ─────────────────────────────────────────────────────────────────────
参考样例（供你照抄一份基线后再改）：

操作系统：Windows 10 IoT Enterprise LTSC 2021 (Build 19044)
Shell：PowerShell 5.1.19041
Git：2.52.0
Python：3.14.0（pip 25.2）
Node.js：v24.12.0（npm 11.7.0，yarn 1.22.22）
───────────────────────────────────────────────────────────────────── -->
</desc_env>
